from setuptools import setup, find_packages

setup(
    name="parse_SQLite",
    version="0.1",
    packages=find_packages(),
    install_requires=[],      
    author="Juan Jovel",     
    description="Series of funcitons to parse the Ecoli.db SQLite db"
)
